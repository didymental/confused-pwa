import { AxiosResponse } from "axios";
import client from "./client";

// export async function listInstructorSessions(data: SignUpRequest): Promise<AxiosResponse> {
//   return client.post<SignUpRequest>("/sessions/", data);
// }
